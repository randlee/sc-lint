boundary_id = "BOUNDARY-DirectiveModel"
owner_package = "sc-lint-directives"
owner_crate_path = "sc_lint_directives"
name = "DirectiveModel"

[public]
facade = "AttributeInput"

[implementation]
type = "AttributeInput"
module = "sc_lint_directives"
visibility = "public"
constructor = "none"

[composition]
roots = []

[dependencies]
allowed_dependents = ["sc-lint-attributes", "sc-lint-boundary", "sc-lint"]
allowed_dependencies = []
forbidden_edges = [
  "sc-lint-directives -> sc-lint-attributes",
  "sc-lint-directives -> sc-lint-boundary",
  "sc-lint-directives -> sc-lint",
]

[references]
scope = "outside_owner_crate"
forbidden = []

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = []

[enforcement]
lint_rules = ["LINT-SC-DIRECTIVES-ISOLATION"]
review_gates = ["no_backend_dependencies"]

[status]
state = "concrete_landed"
