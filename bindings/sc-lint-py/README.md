# sc-lint Python bindings

Wheel-distributed helper package for `sc-lint`. The native extension exposes
only `version_json()` and `run(argv)`; everything else is pure Python. See
`docs/sc-lint/python-bindings.md` in the repository for the contract.
